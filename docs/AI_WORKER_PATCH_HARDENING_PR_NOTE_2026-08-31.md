# PR note

This branch supersedes the older `automation/worker-patch-normalization-timeout` implementation by rebuilding the same bounded hardening from current main after Calendar PR #575 landed. The older branch is retained for traceability and must not be promoted independently.

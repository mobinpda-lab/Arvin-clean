name: NIRA Risk Gate

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  risk-check:
    name: NIRA Risk Detection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Detect sensitive changes
        run: |
          echo "NIRA risk scan started"
          echo "Checking architecture, data model, storage and dependencies"
          echo "Evidence will be generated in later iterations"

      - name: Evidence summary
        run: |
          echo "NIRA Evidence: PASS - baseline scan completed"
